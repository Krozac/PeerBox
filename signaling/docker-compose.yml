version: "3.9"
services:
  signaling:
    build: .
    container_name: signaling-server
    ports:
      - "5501:5501"
      - "5502:5502"
    environment:
      - SIGNALING_PORT=5501
      - API_PORT=5502
      - NODE_ENV=production
    restart: unless-stopped
